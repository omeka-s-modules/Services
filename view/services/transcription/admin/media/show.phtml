<?php
$this->headLink()->appendStylesheet($this->assetUrl('css/transcription/styles.css', 'Services'));
$this->headScript()->appendFile($this->assetUrl('js/transcription/scripts.js', 'Services'));
$this->htmlElement('body')->appendAttribute('class', 'services transcription-project show browse');
?>

<?php echo $this->pageTitle($this->translate('Media'), 1, $this->translate('Services · Transcription'), $media->displayTitle()); ?>

<div class="browse-controls">
    <?php echo $this->pagination(); ?>
    <?php echo $this->sortSelector([
    [
        'label' => $this->translate('Position'),
        'value' => 'position',
    ],
    [
        'label' => $this->translate('Date created'),
        'value' => 'created',
    ],
]); ?>
</div>

<?php echo $this->servicesTranscription()->breadcrumbs(); ?>

<?php if ($pages): ?>

<table class="tablesaw" data-tablesaw-mode="stack">
    <thead>
        <tr>
            <th><?php echo $this->translate('Page'); ?></th>
            <th><?php echo $this->translate('Transcription count'); ?></th>
        </tr>
    </thead>
    <tbody>
    <?php foreach ($pages as $page): ?>
        <tr>
            <td><?php echo $this->hyperlink($page->id(), $this->url('admin/services/transcription-project-page-id', ['page-id' => $page->id()], true)); ?></td>
            <td><?php echo $this->servicesTranscription()->transcriptionCount($page, $project); ?></td>
        </tr>
    <?php endforeach; ?>
    </tbody>
</table>

<div class="browse-controls">
    <?php echo $this->pagination(); ?>
</div>

<?php else: ?>
<div class="no-resources">
    <p><?php echo $this->translate('No pages found.'); ?></p>
</div>
<?php endif; ?>

<div id="project-sidebar" class="sidebar always-open">
    <?php echo $this->partial('services/transcription/admin/media/show-details') ?>
</div>
