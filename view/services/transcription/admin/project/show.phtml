<?php
$this->headLink()->appendStylesheet($this->assetUrl('css/transcription/styles.css', 'Services'));
$this->headScript()->appendFile($this->assetUrl('js/transcription/scripts.js', 'Services'));
$this->htmlElement('body')->appendAttribute('class', 'services transcription-project show browse');

$property = $project->property();
$propertyValue = $property
    ? sprintf(
        '%s: %s',
        $this->translate($property->vocabulary()->label()),
        $this->translate($property->label())
    )
    : '';
switch ($project->target()) {
    case 'items':
        $targetValue = $this->translate('Items');
        break;
    case 'media':
        $targetValue = $this->translate('Media');
        break;
    default:
        $targetValue = $this->translate('Items and media');
        break;
}
?>

<?php echo $this->pageTitle($this->translate('Project'), 1, $this->translate('Services · Transcription'), $project->label()); ?>

<div id="page-actions">
    <div id="page-action-menu">
        <a href="#" class="expand button" aria-label="<?php echo $this->translate('Expand'); ?>"><?php echo $this->translate('Actions'); ?></a>
        <ul class="collapsible">
            <li><?php echo $project->link($this->translate('Edit project'), 'edit'); ?></li>
            <li><a href="#" class="sidebar-content" data-sidebar-selector="#do-preprocess-sidebar"><?php echo $this->translate('Create pages'); ?></a></li>
            <li><a href="#" class="sidebar-content" data-sidebar-selector="#do-transcribe-sidebar"><?php echo $this->translate('Request transcriptions'); ?></a></li>
            <li><a href="#" class="sidebar-content" data-sidebar-selector="#do-save-sidebar"><?php echo $this->translate('Save transcriptions'); ?></a></li>
        </ul>
    </div>
</div>

<?php echo $this->servicesTranscription()->breadcrumbs(); ?>

<h3><?php echo $this->translate('Items'); ?></h3>

<div class="browse-controls">
    <?php echo $this->pagination(); ?>
    <?php echo $this->sortSelector([
        [
            'label' => $this->translate('Date created'),
            'value' => 'created',
        ],
    ]); ?>
</div>

<?php if ($items): ?>

<table class="tablesaw" data-tablesaw-mode="stack">
    <thead>
        <tr>
            <th><?php echo $this->translate('Item'); ?></th>
            <th><?php echo $this->translate('Media count'); ?></th>
            <th><?php echo $this->translate('Page count'); ?></th>
        </tr>
    </thead>
    <tbody>
    <?php foreach ($items as $item): ?>
        <tr>
            <td><?php echo $this->hyperlink($item->displayTitle(), $this->url('admin/services/transcription-project-item-id', ['item-id' => $item->id()], true)); ?></td>
            <td><?php echo count($item->media()); ?></td>
            <td><?php echo $this->servicesTranscription()->pageCount($item); ?></td>
        </tr>
    <?php endforeach; ?>
    </tbody>
</table>

<div class="browse-controls">
    <?php echo $this->pagination(); ?>
</div>

<?php else: ?>
<div class="no-resources">
    <p><?php echo $this->translate('No items found.'); ?></p>
</div>
<?php endif; ?>

<div id="project-sidebar" class="sidebar always-open">
    <?php echo $this->partial('services/transcription/admin/project/show-details') ?>
</div>

<div id="do-preprocess-sidebar" class="sidebar">
    <?php echo $this->hyperlink('', '#', ['class' => 'sidebar-close o-icon-close', 'title' => $this->translate('Close')]); ?>
    <h3><?php echo $this->translate('Create pages'); ?></h3>
    <p><?php echo $this->translate('Create transcribable pages from items in this project.'); ?></p>
    <?php echo $this->form($formDoPreprocess); ?>
</div>

<div id="do-transcribe-sidebar" class="sidebar">
    <?php echo $this->hyperlink('', '#', ['class' => 'sidebar-close o-icon-close', 'title' => $this->translate('Close')]); ?>
    <h3><?php echo $this->translate('Request transcriptions'); ?></h3>
    <p><?php echo $this->translate('Request transcriptions for pages in this project. In addition to an action selected below, this will submit transcription requests for untranscribed pages, and poll the transcription service for updates.'); ?></p>
    <div class="meta-group">
        <h4><?php echo $this->translate('Model ID'); ?></h4>
        <div class="value"><?php echo $project->modelId(); ?></div>
    </div>
    <?php echo $this->form($formDoTranscribe); ?>
</div>

<div id="do-save-sidebar" class="sidebar">
    <?php echo $this->hyperlink('', '#', ['class' => 'sidebar-close o-icon-close', 'title' => $this->translate('Close')]); ?>
    <h3><?php echo $this->translate('Save transcriptions'); ?></h3>
    <p><?php echo $this->translate('Save transcriptions to their target resources.'); ?></p>
    <div class="meta-group">
        <h4><?php echo $this->translate('Transcription property'); ?></h4>
        <div class="value"><?php echo $propertyValue; ?></div>
    </div>
    <div class="meta-group">
        <h4><?php echo $this->translate('Transcription target'); ?></h4>
        <div class="value"><?php echo $targetValue; ?></div>
    </div>
    <?php echo $this->form($formDoSave); ?>
</div>
